# Local agent/editor settings
.claude/

# Dependencies / build (if ever added)
node_modules/
# Anchored so the React build at web/dist (which the cutover serves) is committable.
/dist/

# Env / secrets
.env
.env.*

# OS
.DS_Store
Thumbs.db
